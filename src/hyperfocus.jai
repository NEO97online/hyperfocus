// Configuration for Hyperfocus features.
// To avoid difficulty in rebasing additions to upstream Focus, we're using these constants instead.
// Otherwise the config version stuff gets hairy.

HYPER_SHOW_COMMENT_DIVIDERS :: true;
